<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectav2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_ta_chegando"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_ta_chegando_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectAv2">
        <activity android:name=".AboutUsActivity"></activity>
        <activity android:name=".InitialActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ContaActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".PrincipalActivityTest" />
        <activity android:name=".ForgotActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>